<Page x:Class="TaskFlow.Uno.Views.CategoryTreePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:utu="using:Uno.Toolkit.UI">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Categories" Style="{StaticResource TitleLargeTextBlockStyle}" />

        <!-- Create Form -->
        <Grid Grid.Row="1" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" PlaceholderText="Category name" Text="{Binding NewCategoryName, Mode=TwoWay}" />
            <TextBox Grid.Column="1" PlaceholderText="Description" Text="{Binding NewCategoryDescription, Mode=TwoWay}" />
            <Button Grid.Column="2" Content="Add" Command="{Binding CreateCategory}" Style="{StaticResource FilledButtonStyle}" />
        </Grid>

        <!-- Category Tree -->
        <TreeView Grid.Row="2" ItemsSource="{Binding Categories}">
            <TreeView.ItemTemplate>
                <DataTemplate>
                    <TreeViewItem ItemsSource="{Binding Children}">
                        <Grid ColumnSpacing="12" Padding="4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" Style="{StaticResource BodyStrongTextBlockStyle}" />
                                <TextBlock Text="{Binding Description}" Opacity="0.6" Style="{StaticResource CaptionTextBlockStyle}" />
                            </StackPanel>
                            <TextBlock Grid.Column="1" Text="{Binding IsActive}" Opacity="0.5" VerticalAlignment="Center" />
                            <Button Grid.Column="2" Content="✕"
                                    Command="{Binding DataContext.DeleteCategory, ElementName=CategoryTreePage}"
                                    CommandParameter="{Binding}" />
                        </Grid>
                    </TreeViewItem>
                </DataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
    </Grid>
</Page>
