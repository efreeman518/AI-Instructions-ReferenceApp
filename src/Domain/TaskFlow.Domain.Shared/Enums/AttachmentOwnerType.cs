namespace TaskFlow.Domain.Shared.Enums;

/// <summary>Defines the supported attachment owner type values shared across TaskFlow layers.</summary>
public enum AttachmentOwnerType
{
    TaskItem = 0,
    Comment = 1
}
