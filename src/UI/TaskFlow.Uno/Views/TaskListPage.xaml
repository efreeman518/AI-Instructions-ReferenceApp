<Page x:Class="TaskFlow.Uno.Views.TaskListPage"
    x:Name="TaskListRoot"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:uen="using:Uno.Extensions.Navigation.UI"
      xmlns:utu="using:Uno.Toolkit.UI"
      Background="{ThemeResource BackgroundBrush}">

    <Grid Padding="{utu:Responsive Normal='16,16,16,16', Wide='24,24,24,24'}" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <utu:AutoLayout Spacing="2">
                <TextBlock Text="Tasks" Style="{StaticResource TitleLarge}" />
                <TextBlock Text="Manage and track all your tasks"
                           Style="{StaticResource BodyMedium}"
                           Foreground="{ThemeResource OnSurfaceVariantBrush}" />
            </utu:AutoLayout>
            <Button Grid.Column="1"
                    Content="&#xE710;  New Task"
                    uen:Navigation.Request="TaskForm"
                    Style="{StaticResource FabButtonStyle}" />
        </Grid>

        <!-- Filters — wide -->
        <Grid Grid.Row="1" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0"
                     PlaceholderText="&#xE721;  Search tasks..."
                     Text="{Binding SearchTerm, Mode=TwoWay}" />

            <ComboBox Grid.Column="1" PlaceholderText="Status" Width="150"
                      Visibility="{utu:Responsive Normal=Collapsed, Wide=Visible}">
                <ComboBoxItem Content="All" />
                <ComboBoxItem Content="Open" />
                <ComboBoxItem Content="InProgress" />
                <ComboBoxItem Content="Completed" />
                <ComboBoxItem Content="Blocked" />
                <ComboBoxItem Content="Cancelled" />
            </ComboBox>

            <ComboBox Grid.Column="2" PlaceholderText="Priority" Width="150"
                      Visibility="{utu:Responsive Normal=Collapsed, Wide=Visible}">
                <ComboBoxItem Content="All" />
                <ComboBoxItem Content="Critical" />
                <ComboBoxItem Content="High" />
                <ComboBoxItem Content="Medium" />
                <ComboBoxItem Content="Low" />
            </ComboBox>
        </Grid>

        <!-- Narrow-only filter row -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8"
                    Visibility="{utu:Responsive Normal=Visible, Wide=Collapsed}">
            <ComboBox PlaceholderText="Status" Width="120">
                <ComboBoxItem Content="All" />
                <ComboBoxItem Content="Open" />
                <ComboBoxItem Content="InProgress" />
                <ComboBoxItem Content="Completed" />
                <ComboBoxItem Content="Blocked" />
            </ComboBox>
            <ComboBox PlaceholderText="Priority" Width="120">
                <ComboBoxItem Content="All" />
                <ComboBoxItem Content="Critical" />
                <ComboBoxItem Content="High" />
                <ComboBoxItem Content="Medium" />
                <ComboBoxItem Content="Low" />
            </ComboBox>
        </StackPanel>

        <!-- Task List -->
        <ListView Grid.Row="3"
                  ItemsSource="{Binding Items}"
                  SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource ListItemCardStyle}"
                            uen:Navigation.Request="TaskDetail"
                            uen:Navigation.Data="{Binding}">
                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <CheckBox Grid.Column="0"
                                      IsChecked="{Binding Status, Converter={x:Null}}"
                                      Command="{Binding DataContext.ToggleStatus, ElementName=TaskListRoot}"
                                      CommandParameter="{Binding}"
                                      VerticalAlignment="Center" />

                            <StackPanel Grid.Column="1" Spacing="4" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Title}"
                                           Style="{StaticResource BodyStrongTextBlockStyle}"
                                           TextTrimming="CharacterEllipsis" />
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <FontIcon Glyph="&#xE8B7;" FontSize="12" Opacity="0.5" VerticalAlignment="Center" />
                                    <TextBlock Text="{Binding CategoryName}" Opacity="0.5"
                                               Style="{StaticResource CaptionTextBlockStyle}" />
                                </StackPanel>
                            </StackPanel>

                            <Border Grid.Column="2" Style="{StaticResource BadgeStyle}">
                                <TextBlock Text="{Binding Status}" Style="{StaticResource BadgeTextStyle}" />
                            </Border>

                            <Border Grid.Column="3" Style="{StaticResource BadgeStyle}">
                                <TextBlock Text="{Binding Priority}" Style="{StaticResource BadgeTextStyle}" />
                            </Border>

                            <TextBlock Grid.Column="4" Text="{Binding DueDate}" Opacity="0.5"
                                       Style="{StaticResource CaptionTextBlockStyle}" VerticalAlignment="Center" />
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
