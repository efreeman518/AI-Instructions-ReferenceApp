<Page x:Class="TaskFlow.Uno.Views.TaskListPage"
    x:Name="TaskListRoot"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:uen="using:Uno.Extensions.Navigation.UI"
      xmlns:utu="using:Uno.Toolkit.UI">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Tasks" Style="{StaticResource TitleLargeTextBlockStyle}" VerticalAlignment="Center" />
            <Button Grid.Column="1"
                    Content="New Task"
                    Command="{Binding CreateNew}"
                    Style="{StaticResource FilledButtonStyle}" />
        </Grid>

        <!-- Filters -->
        <Grid Grid.Row="1" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0"
                     PlaceholderText="Search tasks..."
                     Text="{Binding SearchTerm, Mode=TwoWay}" />

            <ComboBox Grid.Column="1" PlaceholderText="Status" Width="140">
                <ComboBoxItem Content="All" />
                <ComboBoxItem Content="Open" />
                <ComboBoxItem Content="InProgress" />
                <ComboBoxItem Content="Completed" />
                <ComboBoxItem Content="Blocked" />
                <ComboBoxItem Content="Cancelled" />
            </ComboBox>

            <ComboBox Grid.Column="2" PlaceholderText="Priority" Width="140">
                <ComboBoxItem Content="All" />
                <ComboBoxItem Content="Critical" />
                <ComboBoxItem Content="High" />
                <ComboBoxItem Content="Medium" />
                <ComboBoxItem Content="Low" />
            </ComboBox>
        </Grid>

        <!-- Task List -->
        <ListView Grid.Row="2"
                  ItemsSource="{Binding Items}"
                  SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="12" ColumnSpacing="12"
                          uen:Navigation.Request="TaskDetail"
                          uen:Navigation.Data="{Binding}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <CheckBox Grid.Column="0"
                                  IsChecked="{Binding Status, Converter={x:Null}}"
                                  Command="{Binding DataContext.ToggleStatus, ElementName=TaskListRoot}"
                                  CommandParameter="{Binding}" />

                        <StackPanel Grid.Column="1" Spacing="2">
                            <TextBlock Text="{Binding Title}" Style="{StaticResource BodyStrongTextBlockStyle}" />
                            <TextBlock Text="{Binding CategoryName}" Opacity="0.6" Style="{StaticResource CaptionTextBlockStyle}" />
                        </StackPanel>

                        <Border Grid.Column="2" Padding="8,4" CornerRadius="4" Background="{ThemeResource SystemControlBackgroundBaseLowBrush}">
                            <TextBlock Text="{Binding Status}" Style="{StaticResource CaptionTextBlockStyle}" />
                        </Border>

                        <Border Grid.Column="3" Padding="8,4" CornerRadius="4" Background="{ThemeResource SystemControlBackgroundBaseLowBrush}">
                            <TextBlock Text="{Binding Priority}" Style="{StaticResource CaptionTextBlockStyle}" />
                        </Border>

                        <TextBlock Grid.Column="4" Text="{Binding DueDate}" Opacity="0.6"
                                   Style="{StaticResource CaptionTextBlockStyle}" VerticalAlignment="Center" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
