namespace TaskFlow.Uno.Views;

/// <summary>Hosts the task list XAML view and initializes its Uno page or control.</summary>
public sealed partial class TaskListPage : Page
{
    /// <summary>Initializes task list page with required dependencies and default state.</summary>
    public TaskListPage()
    {
        this.InitializeComponent();
    }
}
