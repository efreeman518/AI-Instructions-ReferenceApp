namespace TaskFlow.Uno.Views;

/// <summary>Hosts the dashboard XAML view and initializes its Uno page or control.</summary>
public sealed partial class DashboardPage : Page
{
    /// <summary>Initializes dashboard page with required dependencies and default state.</summary>
    public DashboardPage()
    {
        this.InitializeComponent();
    }
}
